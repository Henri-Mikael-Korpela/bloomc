main :: proc() ->
    a := [const]Int{ 2, 5, 9 }
    b := a[1] + 1 + a[0]
    printf("A: %i, %i, %i\n", a[0], b, a[2])

    c := [const]Int {
        1, 2,
        2..<4 = 3,
        4..<8 = 4
    }
    printf("C: %i, %i, %i\n", c[2], c[3], c[4])
    printf("C length: %i\n", length(c))

    d := [2]Int { 6, 12 }
    printf("D: #1: %i, #2: %i\n", d[0], d[1])