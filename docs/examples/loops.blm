main :: proc() ->
    i := 0
    for ->
        if i == 5 ->
            break
        i += 1
    printf("First loop passed!\n")

    for i in 0..<10 ->
        printf("Index: %i\n", i)
    
    j := 0
    for j < 10 ->
        printf("j = %i\n", j)
        j += 1
