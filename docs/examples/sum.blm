sum :: proc(a : Int, b : Int) Int ->
    a + b

sum2 :: proc(a : Int, b : Int) Int ->
    a + b

main :: proc() ->
    x := 501
    printf("My number: %i\n", x)
    y := 202
    printf("My other number: %i\n", y)
